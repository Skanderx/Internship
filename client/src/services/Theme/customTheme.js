import { createTheme } from "@mui/material/styles";

export const customTheme = createTheme({
  palette: {
    primary: {
      main: '#696969' ,
    },
    secondary: {
      main: '#4B0082',
    },
  },
});
