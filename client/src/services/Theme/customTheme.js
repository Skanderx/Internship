import { createTheme } from "@mui/material/styles";

export const customTheme = createTheme({
  palette: {
    primary: {
      main: '#6495ED' ,
    },
    secondary: {
      main: '#008000',
    },
  },
});
